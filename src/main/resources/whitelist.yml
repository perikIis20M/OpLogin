# OpLogin Whitelist Configuration
# List of commands that can be used without requiring login
# Format: Each command should be written without the '/' prefix
whitelisted-commands:
  - help
  - oplogin
  - list
  - ping 